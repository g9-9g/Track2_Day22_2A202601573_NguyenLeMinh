# ── LangChain core ────────────────────────────────────────────────
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<1.0.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<1.0.0

# ── LLM Providers (cài tất cả để hỗ trợ đa provider) ─────────────
langchain-openai>=0.3.0,<1.0.0   # OpenAI + OpenRouter
langchain-anthropic>=0.3.0,<1.0.0 # Anthropic Claude
langchain-google-genai>=2.0.0,<3.0.0 # Google Gemini
langchain-ollama>=0.2.0,<1.0.0   # Ollama (local)

# ── LangSmith ─────────────────────────────────────────────────────
langsmith>=0.2.0

# ── Vector store ──────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────
ragas==0.4.3
datasets>=2.0.0                  # required by RAGAS internally
numpy>=1.25.0

# ── Guardrails AI ─────────────────────────────────────────────────
guardrails-ai==0.6.6
litellm==1.80.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai==1.109.1                  # OpenAI SDK (compatible with LangChain 0.3)
tiktoken>=0.5.0                  # token counting
